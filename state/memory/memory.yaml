# Working Memory — Cross-session continuity
# Schema per entry:
#   id: mem_NNN           # unique identifier
#   content: string       # what to remember
#   scope: string         # project:<name>, area:<name>, or global
#   created: date         # YYYY-MM-DD
#   source: string        # session journal reference
#   decay: string         # persistent | 30d | 7d | session
#   tags: [string]        # categorization tags
#
# Capacity: soft limit 50, hard warning at 100

entries: []
